  if (y < 300) {
    Serial.println("hello oben);
  }

  // Unten
  else if (y > 700) {
    Serial.println("hello unten");
  }

  // Links
  else if (x < 300) {
    Serial.println("hello links");
  }

  // Rechts
  else if (x > 700) {
    Serial.println("hello rechts");
  }

  // Button (Mitte drücken)
  else if (button == LOW) {
    Serial.println("hello mitte");
  }